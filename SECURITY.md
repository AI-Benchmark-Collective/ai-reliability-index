# Security Policy

Do not disclose credentials, private evaluation data, prompt-injection payloads with
live targets, or vulnerabilities in public issues.

For now, report security concerns privately to an organization owner through GitHub.
The project will publish a dedicated security contact before accepting security-sensitive
benchmark tracks.

The inaugural task set must remain synthetic, non-destructive, and offline by default.

